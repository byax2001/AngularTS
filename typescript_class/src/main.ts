import './style.css'
// se esta importando un modulo  que es una agrupación de código que se puede reutilizar en otros archivos
import './topics/basicos' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/objetos_arreglos_interfaces' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/functions' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/desestructuring_objects' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/destructuring_arrays' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/destructuring_functions' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/clases' //Con solo importar el archivo se ejecuta el código que se vera en la consola
import './topics/genericos'
//import './topics/decoradores'
import './topics/optional_chaining'

document.querySelector<HTMLDivElement>('#app')!.innerHTML = `Hola Mundo`


